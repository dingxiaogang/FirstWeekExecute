<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <android.support.v4.view.ViewPager
        android:id="@+id/main_viewPager"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">
    </android.support.v4.view.ViewPager>

    <RadioGroup
        android:id="@+id/main_rg"
        android:layout_width="match_parent"
        android:layout_height="90dp"
        android:orientation="horizontal">
        <RadioButton
            android:id="@+id/main_rb_story"
            android:text="@string/story"
            android:textColor="@drawable/selector_button_picture"
            android:drawableTop="@drawable/selector_button_picture"
            android:button="@null"
            android:gravity="center"
            android:textSize="18dp"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent" />
        <RadioButton
            android:id="@+id/main_rb_parent_child"
            android:text="@string/parent_child"
            android:textColor="@drawable/selector_button_color"
            android:drawableTop="@drawable/selector_button2_picture"
            android:button="@null"
            android:gravity="center"
            android:textSize="18dp"
            android:layout_width="0dp"
            android:layout_weight="1"
            android:layout_height="match_parent" />
    </RadioGroup>

</LinearLayout>